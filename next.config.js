/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'upload.wikimedia.org',
            'picsum.photos',
            'utfs.io',
            'i.pravatar.cc',
        ],
    },
}

module.exports = nextConfig
